spring:
  application:
    name: 022-config-server
#日志
logging:
  level:
    org:
      springframework:
        security: DEBUG
#web服务端口
server:
  port: 8900
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mylava/springcloud-config-repo.git
